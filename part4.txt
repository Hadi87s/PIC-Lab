#include <Servo.h>  

Servo Servo1;  

void setup() {
  Servo1.attach(2);
}

void loop() {

  for (int i = 0; i <= 90; i++) {
    Servo1.write(i);  
    delay(20);            
  }
 delay(2000);
  for (int j = 90; j <= 180; j++) {
    Servo1.write(j);  
    delay(40);           
  }
}
